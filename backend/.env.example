PORT=4000
NODE_ENV=development
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/horse-racing?retryWrites=true&w=majority
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:3000
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!

# Public API (x-api-key required for /api/v1/public/*). Keep secret.
# PUBLIC_API_KEY=your-secret-public-api-key

# Auto-sync results from Casa every 10 min (runs even when no one is logged in)
# AUTO_SYNC_ENABLED=false
# AUTO_SYNC_INTERVAL_MS=600000
